apiVersion: v1
kind: Service
metadata:
    name: "quizapp-c813"
    labels:
        app: "quizapp-c813"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "quizapp-c813"